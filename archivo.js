function cambiarColor(color) {
    var body = document.getElementById('body')
    body.style.background = color.value
}

function cambiarColorLista () {
    let coloresLista = document.getElementById("lista")

    for (let i = 1; i < coloresLista.length; i++) {
        const element = coloresLista[i]
        if (i %2 === 0) {
            coloresLista.element
        }
        
    }
}